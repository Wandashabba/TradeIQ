/// Squared-off geometry tokens — controls 3px, panels 4px, nothing is a pill.
/// Mode-independent (geometry doesn't flip with the theme).
class TiqGeometry {
  TiqGeometry._();
  static const double control = 3;
  static const double panel = 4;
}
