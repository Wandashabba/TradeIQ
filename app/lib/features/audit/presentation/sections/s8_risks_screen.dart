import 'package:flutter/material.dart';

class S8RisksScreen extends StatelessWidget {
  const S8RisksScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('S8 Opportunities & Risks'));
}
