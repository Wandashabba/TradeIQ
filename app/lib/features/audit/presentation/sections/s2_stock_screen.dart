import 'package:flutter/material.dart';

class S2StockScreen extends StatelessWidget {
  const S2StockScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('S2 Stock & Availability'));
}
