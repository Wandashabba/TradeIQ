import 'package:flutter/material.dart';

class S7CapabilityScreen extends StatelessWidget {
  const S7CapabilityScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('S7 Sales Capability'));
}
