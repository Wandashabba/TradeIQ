import 'package:flutter/material.dart';

class S1OutletInfoScreen extends StatelessWidget {
  const S1OutletInfoScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('S1 Outlet Information'));
}
