import 'package:flutter/material.dart';

class S10ScorecardScreen extends StatelessWidget {
  const S10ScorecardScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('S10 Execution Scorecard'));
}
