# Premium UI Sub-5c — Guided Capture, Outcome, My-Work, Motion — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finish the field-agent premium redesign — the **guided-launch capture** moment, the **score-reveal outcome** and **my-work** screens on the console system (theme-aware light + dark), and a **motion pass** — completing sub-project 5.

**Architecture:** Three pieces. (1) **Guided capture** — the existing `PhotoCaptureField` inline tile becomes a single "Add photo" affordance that opens a NEW full-screen `GuidedCaptureScreen` (section name + framing hint + bracket graphic + a big Capture button that launches the existing `PhotoCaptureService.capture(camera)`, plus a "Choose from gallery" secondary), then a keep/retake review, returning the photo through the unchanged `onCaptured` contract. **User decision (recorded):** the guided-*launch* wrapper — NO new camera dependency; reuse `image_picker`/`PhotoCaptureService` and the offline queue exactly. (2) **Outcome + my-work** — both kit-scaffolded screens still on static `AppColors`; convert to `context.colors` + console treatment (glass-hero score reveal; worklist my-work). (3) **Motion pass** — verify/round out the one-shot entrances + reduceMotion across the agent flow. No provider/data/geofence changes; keys preserved.

**Tech Stack:** Flutter; Riverpod; `flutter_test`; `context.colors`; console `PanelCard`/glass-hero; existing `PhotoCaptureService` (`image_picker`); pinned Chrome-for-Testing for what's reachable.

**Spec:** `docs/superpowers/specs/2026-07-25-premium-ui-sub5-agent-app-design.md` §Screens 5 (capture), 7 (outcome), 8 (my-work), §Motion, §Scope 5c.

**Branch:** `feat/premium-ui-sub5c` off fresh `main` (has 5a + 5b-1; 5b-2/#219 is independent — no file overlap). PR bases on `main`.

**Verified facts (code-grounded; don't re-derive):**
- **`PhotoCaptureField`** `app/lib/core/widgets/photo_capture_field.dart` (theme-aware already): empty state = a `ValueKey('photo-capture-tile')` Container with two `OutlinedButton.icon` — `photo-camera` (→ `_capture(PhotoSource.camera)`) and `photo-gallery` (→ gallery) — + busy spinner; captured state = `_Preview` (92×92 `photo-preview` + `photo-remove` Retake). `_capture(source)` calls `ref.read(photoCaptureServiceProvider).capture(source)` → `photo.dataUrl`, then `widget.onCaptured(dataUrl)`; handles `PhotoTooLargeException` + permission errors into `_error` (`photo-error`). `onCaptured(String?)` is the contract sections depend on (they feed it to `queuePhoto`). Keys: `photo-capture-tile/camera/gallery/preview/remove/error`.
- **`PhotoCaptureService`** `app/lib/core/camera/photo_capture_service.dart`: `capture(PhotoSource source)` → `CapturedPhoto?` (`.dataUrl`), downscale 1600 / q80, 8MB cap (`PhotoTooLargeException`), `null` on cancel. `PhotoSource{camera, gallery}`. Provider `photoCaptureServiceProvider`; seam `ImagePickerGateway` for tests. DO NOT change this — the guided screen calls it.
- **Visit outcome** `app/lib/features/audit/presentation/visit_outcome_screen.dart`: kit-scaffolded (`AgentScaffold`, `AgentButton(ValueKey('next-store'))`, `AnimatedCount`, `TickMark`, `StatusBanner`, `Reveal`) but uses static `AppColors.*` throughout. States: `_Scoring` (loading), `_HeldOnPhone` (offline receipt — honest "no score guessed" copy), `_Scored` (52px `AnimatedCount` score /100, band dot+word, `_Delta` vs last visit, "How it was scored" dimension bars with `_HatchPainter` for unmeasured). **Band `_band()` uses `AppColors.good/warn/crit` as TEXT colour — `crit` as text fails 4.5:1; must become `critText` (the recurring AA lesson).** Watches `visitOutcomeProvider`. Honesty copy (no phone-side score guessing, "—" not 0 for unmeasured #93) VERBATIM.
- **My-work** `app/lib/features/audit/presentation/my_work_screen.dart`: READ IT. Sync queue — pending / needs-attention / sent, kit-based, `AppColors` static, `syncStatusProvider`. Route `/my-work` (shared). Get its exact structure/keys from the file + its test if one exists.
- `context.colors` tokens: `plane, surface1, surface2, surface3, line, lineStrong, ink1, ink2, ink3, brand, good, warn, crit, critText, series1, heroWash, heroBorder`. Geometry `AppColors.radiusPanel/radiusControl` stays. The console glass-hero: `PanelCard(gradient: heroWash→surface1, borderColor: heroBorder)` (see Today's `_GlassHero`, on main).
- `AppColors.*→context.colors` is dark-identical + adds light (TiqColors.dark seeded from AppColors).
- Motion primitives (`agent_motion.dart`): `Reveal`, `AnimatedCount`, `PressFeedback`, `TickMark`, `reduceMotion`, `Motion` (`fast/base/slow/enter/stagger`). Camera-permission/offline behaviour unchanged.
- `contrastRatio`: `app/test/core/theme/tiq_colors_test.dart`. Tests: `visit_outcome_screen_test.dart`, `my_work_screen_test.dart`(?), `photo_capture_field_test.dart`, `photo_capture_service_test.dart`. Preserve keys.

---

## Task 1: `GuidedCaptureScreen` + rewire the capture tile

**Files:** create `app/lib/core/widgets/guided_capture_screen.dart`; modify `photo_capture_field.dart`; add `app/test/core/widgets/guided_capture_screen_test.dart`; update `photo_capture_field_test.dart`.

- [ ] **Step 1 — Failing tests (both themes):** (a) `GuidedCaptureScreen({required String label, required String hint})` renders full-screen with the section `label`, the framing `hint` text, a framing-bracket graphic, a primary Capture `AgentButton` (`ValueKey('guided-capture')`) and a secondary "Choose from gallery" (`ValueKey('guided-gallery')`); tapping Capture calls `PhotoCaptureService.capture(PhotoSource.camera)` (fake the service), gallery calls `capture(gallery)`; a non-null result pops the route with the `dataUrl`; cancel (`null`) stays; a `PhotoTooLargeException`/error renders an inline error (not a crash); on `context.colors` both themes. (b) `PhotoCaptureField`: the empty tile now shows a single "Add photo" affordance (`ValueKey('photo-capture-tile')` kept) that PUSHES `GuidedCaptureScreen`; on return with a dataUrl the field shows the `_Preview` and calls `onCaptured`; the old `photo-camera`/`photo-gallery` inline buttons are gone (or repurposed — assert the new flow). `onCaptured` contract + `photo-preview`/`photo-remove`/`photo-error` keys preserved.
- [ ] **Step 2 — Watch fail.**
- [ ] **Step 3 — Implement:** `GuidedCaptureScreen` — a full-screen route (dark-ish capture chrome is fine even in light theme — a camera surface; but use `context.colors` tokens, not hardcoded, so it's coherent; the framing brackets a simple `CustomPaint`/bordered corners in `brand`/`ink`); Capture → `capture(camera)`, gallery → `capture(gallery)`, pop with dataUrl; errors inline (reuse the field's error copy). `PhotoCaptureField._capture` refactored: the tile's "Add photo" pushes the guided screen (`Navigator.push`/an `agentSectionRoute`-style route) and awaits the dataUrl; keep the `PhotoTooLargeException`/permission handling; keep the `_Preview`/retake path (Retake re-opens the guided screen). Offline queue path unchanged (the field still calls `onCaptured`; sections still `queuePhoto`). Preserve keys where possible; note any changed/removed key + update tests.
- [ ] **Step 4 — Verify + commit:** `cd app && flutter test test/core/widgets/ && flutter analyze`, full `flutter test`. `dart format`. Grep-guard non-geometry `AppColors.` in touched files. Commit: `feat(app): guided full-screen capture launch (premium-ui sub5c)` + trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## Task 2: Visit outcome — glass-hero score reveal

**Files:** modify `visit_outcome_screen.dart`; modify `visit_outcome_screen_test.dart`.

- [ ] **Step 1 — Failing tests (both themes):** (a) `_Scored` renders the score in a **glass hero** (heroWash→surface1 gradient + heroBorder), the 52px `AnimatedCount` in `ink1`, "/100" in ink3; the band = dot + spelled-out word (Green/Amber/Red) with the WORD's text colour AA-safe on its ground — **red uses `critText`, not `crit`** — assert the rendered band-text pair ≥4.5:1 for all three bands both themes; (b) `_Delta` up/down carries a word + arrow (never colour-alone) and its coloured text clears 4.5:1 both themes (down uses `critText`); (c) the "How it was scored" dimension list on console tokens, `_HatchPainter` for unmeasured (theme-aware), "—" not 0 (#93) preserved; (d) `_HeldOnPhone`/`_Scoring` on `context.colors`, the no-phone-side-score honesty copy VERBATIM; (e) grep-guard no non-geometry `AppColors.`; keep `ValueKey('next-store')`.
- [ ] **Step 2 — Watch fail.**
- [ ] **Step 3 — Implement:** `AppColors.*→context.colors`; wrap the score in a `PanelCard` glass hero; band + delta text → AA-safe (`good`/`warn`/`critText`; the dot may keep the raw token as a mark); dimension bars `series1`/`surface3` themed, hatch `lineStrong`; `_HeldOnPhone` `StatusBanner`/tick/copy themed + verbatim; keep `AnimatedCount`/`Reveal`/reduceMotion, `visitOutcomeProvider`, routing, keys. Honesty: never guess a phone-side score; "—" for unmeasured; band never colour-alone.
- [ ] **Step 4 — Verify + commit:** `flutter test test/features/audit/visit_outcome_screen_test.dart && flutter analyze`, full `flutter test`. `dart format`. Commit: `feat(app): visit outcome score reveal on the console system (premium-ui sub5c)` + trailer.

## Task 3: My-work sync queue on the console system

**Files:** modify `app/lib/features/audit/presentation/my_work_screen.dart`; modify its test (`my_work_screen_test.dart` if present — else add one).

- [ ] **Step 1 — Read the file** — its structure (pending / needs-attention / sent groups), keys, `syncStatusProvider` wiring, retry affordances, and the honest sync copy (offline = normal, not error).
- [ ] **Step 2 — Failing tests (both themes):** (a) queue items render as console cards/worklist rows on `context.colors` (surface1 + line + radiusPanel); no raw `Card`/`ListTile`/`ElevatedButton` if any; (b) status indicators (pending/needs-attention/sent) carry a word + glyph (never colour-alone) and any coloured text clears 4.5:1 both themes (needs-attention likely `critText`/`warn`); (c) retry affordance → `AgentButton`; (d) grep-guard non-geometry `AppColors.`; (e) sync copy + behaviour (provider wiring, retry) preserved.
- [ ] **Step 3 — Watch fail, implement:** `AppColors.*→context.colors`; groups → console cards/`StatusBanner`; retry → `AgentButton`; keep `syncStatusProvider` wiring, keys, the "held on phone is normal" honesty copy verbatim.
- [ ] **Step 4 — Verify + commit:** `flutter test test/features/audit/ && flutter analyze`, full `flutter test`. `dart format`. Commit: `feat(app): my-work sync queue on the console system (premium-ui sub5c)` + trailer.

## Task 4: Motion pass + full verification + browser proof + PR

- [ ] **Step 1 — Motion pass:** audit the agent flow for the spec's motion table — one-shot entrances (`Reveal`), count-ups (outcome score, hub progress — already present), reduceMotion-static everywhere. Add any missing one-shot entrance on the outcome/my-work lists; confirm NOTHING loops (the only sanctioned loop is the manager maps' pin-glow). Add a reduceMotion test on the outcome (score at final value on first frame). Small commit if anything changed: `feat(app): agent motion pass — one-shot entrances, reduceMotion (premium-ui sub5c)` + trailer.
- [ ] **Step 2 — Full gate:** `cd app && flutter test && flutter analyze && flutter build web --release`.
- [ ] **Step 3 — Browser proof (both themes, phone 420×900).** Pinned CFT Chrome; field_agent demo (`agent@demo-fmcg.tradeiq.com`/`demo-password-123`). **My-work** (`/my-work`) is likely reachable without a check-in — screenshot it light + dark. The **outcome** needs a submitted visit and the **guided capture** needs a section open — both behind the web-unreachable local-DB check-in (#177/#140, as 5b): rely on their both-theme widget tests and say so; screenshot whatever IS reachable (my-work, and the guided-capture screen if it can be pushed directly or via a test harness). LOOK at every shot.
- [ ] **Step 4 — PR:** base `main`, title `feat: guided capture, score reveal, my-work + motion — completes the agent app (premium-ui sub-5c)`. Body: the guided-launch capture (no new dependency — user decision), the glass-hero outcome, my-work, the motion pass; honesty carry-forwards (no phone-side score guess, "—" not 0, offline-is-normal, band/delta never colour-alone, AA on all bands both themes); both-theme coverage; the web-unreachable limit; note this **completes sub-project 5** (the full field-agent premium redesign) — 5a+5b+5c.

## Self-review notes
- Spec coverage: guided capture ✓(T1, launch-wrapper per user decision — no camera dependency), outcome score reveal ✓(T2), my-work ✓(T3), motion ✓(T4). This is the last 5c piece — sub-5 complete after merge.
- Honesty: no phone-side score guessing (verbatim), "—" not 0 (#93), offline-is-a-receipt, band/delta/status never colour-alone, AA on all bands both themes (the crit→critText lesson).
- No provider/data/geofence/camera-pipeline changes; `PhotoCaptureService`/`onCaptured`/offline queue untouched; keys preserved.
- Deferred: #214/#215 shared-widget extractions remain open.
