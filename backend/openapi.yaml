openapi: 3.0.3
info:
  title: TradeIQ API
  version: 0.1.0
  description: >
    Phase 1 (complete). Implemented: /auth, /outlets, /skus, /visits (check-in
    + submit + list), /stock (S2), /visibility (S3-S4, vision-stub wired),
    /pricing (S5, OCR-stub wired), /competitive (S6), /capability (S7), /risks
    (S8), /tasks (S9), /scorecards (S10), /dashboard, and /photos (photo
    capture + photo-verified task closure). Every mounted route is implemented
    — no route returns 501. Manager-facing GET listings on the per-visit
    section modules follow a common contract: GET /<section>?visitId=<id>
    returns that visit's captured rows (any authenticated role, tenant-scoped,
    400 if visitId is missing, 404 if the visit is not the caller's). All
    non-auth/non-health routes require a Bearer JWT; write routes are
    role-guarded (403 when the caller's role is not permitted).
    Phase 3 (Activation) adds /territories, /campaigns, /beatplans, /alerts,
    /templates, and /trends — see docs/architecture/phase3-activation.md.
    Phase 2 (Intelligence) adds /fraud, /dispatch, /forecast; Phase 3 Tier-2
    adds /orders, /messages, /announcements, /webhooks, /gamification, /reports
    — see docs/architecture/phase2-intelligence.md.
servers:
  - url: http://localhost:4000
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
  /auth/login:
    post:
      summary: Log in and receive a JWT
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string }
                password: { type: string }
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  token: { type: string }
                  role: { type: string, enum: [field_agent, manager, admin] }
        '401':
          description: Invalid credentials
  /outlets:
    get:
      summary: List outlets for the authenticated user's client
      security: [{ bearerAuth: [] }]
      responses:
        '200':
          description: List of outlets
          content:
            application/json:
              schema:
                type: array
        '401':
          description: Missing or invalid token
    post:
      summary: Create an outlet
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, code, channelType, lat, lng, territoryId]
              properties:
                name: { type: string }
                code: { type: string }
                channelType: { type: string }
                lat: { type: number }
                lng: { type: number }
                territoryId: { type: string }
                teamProfile: { type: object }
      responses:
        '201':
          description: Outlet created
          content:
            application/json:
              schema:
                type: object
        '400':
          description: Missing required fields (name, code, channelType, lat, lng, territoryId)
        '401':
          description: Missing or invalid token
        '403':
          description: Caller's role may not create outlets (manager/admin only)
        '409':
          description: An outlet with this code already exists
  /skus:
    get:
      summary: List SKUs for the authenticated user's client
      security: [{ bearerAuth: [] }]
      responses:
        '200':
          description: List of SKUs
          content:
            application/json:
              schema:
                type: array
        '401':
          description: Missing or invalid token
  /visits:
    get:
      summary: List visits (field_agent sees own; manager/admin sees the client's)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: outletId
          in: query
          schema: { type: string }
        - name: status
          in: query
          schema: { type: string, enum: [in_progress, submitted] }
      responses:
        '200':
          description: Visits (newest check-in first)
        '400':
          description: Invalid status filter
        '401':
          description: Missing or invalid token
    post:
      summary: Geofenced outlet check-in (creates a Visit) — field_agent only
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [outletId, lat, lng]
              properties:
                outletId: { type: string }
                lat: { type: number }
                lng: { type: number }
                checkinTs:
                  type: string
                  format: date-time
                  description: Client-captured check-in time (offline-first); server clock if omitted.
      responses:
        '201':
          description: Visit created (status in_progress)
        '400':
          description: Missing outletId, lat, or lng
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Outlet not found for this client
        '422':
          description: Check-in location is outside the outlet geofence
  /visits/{id}:
    get:
      summary: One visit, for manager review — manager/admin, tenant-scoped (#208)
      description: >
        Outlet, agent, device check-in and submit times, geofence distance, the
        perfect-store score with every dimension (null = not measurable), a
        summary per capture section, photo METADATA with thumbnail URLs, and the
        visit's fraud signals. Photo bytes are never included; fetch each
        thumbnail from `thumbnailUrl` (GET /photos/{id}/thumbnail). A draft
        (in_progress) visit returns `submittedAtClient: null` and, until scored,
        `score: null`. Section rows are read up to 500 each and photos up to
        200; `count`/`total` are always exact and `truncated` marks a section
        whose findings came from fewer rows than exist.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: The visit detail
          content:
            application/json:
              schema:
                type: object
                required: [id, status, outlet, agent, checkinTs, submittedAtClient, geofence, score, sections, photos, fraud]
                properties:
                  id: { type: string }
                  status: { type: string, enum: [in_progress, submitted] }
                  outlet:
                    type: object
                    properties:
                      id: { type: string }
                      name: { type: string }
                      code: { type: string }
                      channelType: { type: string }
                  agent:
                    type: object
                    description: User has no display-name column, so the email is the agent's name.
                    properties:
                      id: { type: string }
                      email: { type: string }
                  checkinTs: { type: string, format: date-time, description: Device clock at check-in }
                  submittedAtClient:
                    type: string
                    format: date-time
                    nullable: true
                    description: Device clock at submit; null for a draft or a visit recorded before it existed
                  geofence:
                    type: object
                    properties:
                      pass: { type: boolean }
                      distanceM: { type: number, nullable: true }
                  score:
                    type: object
                    nullable: true
                    properties:
                      weightedTotal: { type: number }
                      ratingBand: { type: string, enum: [green, amber, red] }
                      target: { type: number, description: "The client's current green threshold" }
                      scoredAt: { type: string, format: date-time }
                      dimensions:
                        type: array
                        items:
                          type: object
                          properties:
                            key: { type: string, enum: [availability, visibility, display, pricing, competitive, salesCapability] }
                            score: { type: number, nullable: true }
                  sections:
                    type: array
                    description: Always the five sections, in this order - stock, visibility, pricing, competitive, risks.
                    items:
                      type: object
                      properties:
                        key: { type: string, enum: [stock, visibility, pricing, competitive, risks] }
                        count: { type: integer }
                        flagged: { type: integer, description: Rows needing attention }
                        findings: { type: array, maxItems: 3, items: { type: string } }
                        truncated: { type: boolean }
                  photos:
                    type: object
                    properties:
                      total: { type: integer }
                      items:
                        type: array
                        items:
                          type: object
                          properties:
                            id: { type: string }
                            section: { type: string }
                            timestamp: { type: string, format: date-time }
                            thumbnailUrl: { type: string, example: /photos/3f2a.../thumbnail }
                  fraud:
                    type: object
                    properties:
                      riskScore: { type: number, minimum: 0, maximum: 100 }
                      signals:
                        type: array
                        items:
                          type: object
                          properties:
                            code: { type: string }
                            detail: { type: string }
                            weight: { type: number }
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a manager or admin
        '404':
          description: Visit not found for this client
  /visits/{id}/submit:
    post:
      summary: Mark a visit submitted — field_agent, owner only
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Visit updated (status submitted)
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Visit not found for this client/agent
  /stock:
    post:
      summary: Record per-SKU stock for a visit (S2) — field_agent only
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [visitId, items]
              properties:
                visitId: { type: string }
                items:
                  type: array
                  items:
                    type: object
                    required:
                      [skuId, unitsAvailable, lastStockinDate, daysOutOfStock, velocityAvg, salesActual, salesTarget]
                    properties:
                      skuId: { type: string }
                      unitsAvailable: { type: integer }
                      lastStockinDate: { type: string, format: date-time }
                      daysOutOfStock: { type: integer }
                      velocityAvg: { type: number }
                      salesActual: { type: number }
                      salesTarget: { type: number }
      responses:
        '201':
          description: Stock rows created (coverageDaysPredicted computed server-side)
        '400':
          description: Missing visitId or a non-empty valid items[]
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Visit or a referenced SKU not found for this client
  /visibility:
    post:
      summary: Record visibility/display for a visit (S3-S4) — field_agent only
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                [visitId, brandingElements, planogramCompliancePct, facingsCount, highTrafficPass, cleanlinessScore]
              properties:
                visitId: { type: string }
                brandingElements: { type: object }
                planogramCompliancePct: { type: number }
                facingsCount: { type: object }
                highTrafficPass: { type: boolean }
                cleanlinessScore: { type: integer }
      responses:
        '201':
          description: Visibility upserted (one row per visit)
        '400':
          description: Missing or invalid required fields
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Visit not found for this client
  /pricing:
    post:
      summary: Record per-SKU shelf pricing for a visit (S5) — field_agent only
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [visitId, items]
              properties:
                visitId: { type: string }
                items:
                  type: array
                  items:
                    type: object
                    required: [skuId, priceActual, promoActive, promoMaterialsDetected, commsRating]
                    properties:
                      skuId: { type: string }
                      priceActual:
                        type: number
                        description: Manual shelf-price entry (OCR-stub passthrough; real OCR is Phase 2, issue #2).
                      promoActive: { type: boolean }
                      promoMaterialsDetected: { type: object }
                      commsRating: { type: integer }
      responses:
        '201':
          description: Pricing rows created (priceMaster from Sku.rrp, deviationPct computed server-side)
        '400':
          description: Missing visitId or a non-empty valid items[]
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Visit or a referenced SKU not found for this client
  /competitive:
    post:
      summary: Record competitor observations for a visit (S6) — field_agent only
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [visitId, items]
              properties:
                visitId: { type: string }
                items:
                  type: array
                  items:
                    type: object
                    required: [competitorSku, competitorPrice, competitorPosmType, competitorPromoterPresent, geotag]
                    properties:
                      competitorSku: { type: string }
                      competitorPrice: { type: number }
                      competitorPosmType: { type: string }
                      competitorPromoterPresent: { type: boolean }
                      geotag: { type: object }
      responses:
        '201':
          description: Competitive rows created
        '400':
          description: Missing visitId or a non-empty valid items[]
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Visit not found for this client
  /capability:
    post:
      summary: Record sales-team capability for a visit (S7) — field_agent only
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [visitId, staffHeadcountConfirmed, repTrainingStatus, quizScore]
              properties:
                visitId: { type: string }
                staffHeadcountConfirmed: { type: integer }
                repTrainingStatus: { type: object }
                quizScore: { type: integer }
      responses:
        '201':
          description: Capability upserted (one row per visit)
        '400':
          description: Missing or invalid required fields
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Visit not found for this client
  /risks:
    post:
      summary: Flag risks for a visit (S8) — field_agent only; auto-creates SLA tasks
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [visitId, risks]
              properties:
                visitId: { type: string }
                risks:
                  type: array
                  items:
                    type: object
                    required: [flagType, severity, note]
                    properties:
                      flagType: { type: string }
                      severity: { type: string, enum: [critical, high, normal] }
                      note: { type: string }
      responses:
        '201':
          description: >
            Risk rows created, plus one auto-created Task per risk (owner = the
            visit's agent, slaDueAt from severity: critical=+24h, high=+3d,
            normal=+7d). Returns { risks, tasks }.
        '400':
          description: Missing visitId, empty risks[], or invalid severity
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Visit not found for this client
  /tasks:
    post:
      summary: Create a task manually (S9) — field_agent or manager
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [outletId, findingType, requiredFix, priority]
              properties:
                outletId: { type: string }
                findingType: { type: string }
                requiredFix: { type: string }
                priority: { type: string, enum: [critical, high, normal] }
                visitId: { type: string }
                ownerId: { type: string, description: Defaults to the caller. }
      responses:
        '201':
          description: Task created (slaDueAt computed from priority)
        '400':
          description: Missing or invalid required fields
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent or manager
        '404':
          description: Outlet, visit, or owner not found for this client
    get:
      summary: List tasks for the caller's client, ordered by SLA due date
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          schema: { type: string, enum: [open, in_progress, closed] }
        - name: priority
          in: query
          schema: { type: string, enum: [critical, high, normal] }
        - name: outletId
          in: query
          schema: { type: string }
      responses:
        '200':
          description: Tasks scoped to the caller's client
        '400':
          description: Invalid filter value
        '401':
          description: Missing or invalid token
  /tasks/{id}:
    patch:
      summary: Update task lifecycle (open → in_progress → closed) — field_agent or manager
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status: { type: string, enum: [open, in_progress, closed] }
                closurePhotoUrl: { type: string }
                closureVerified: { type: boolean, description: Manager only. }
      responses:
        '200':
          description: Task updated
        '400':
          description: Invalid body, or closing without a closure photo
        '401':
          description: Missing or invalid token
        '403':
          description: Role not permitted (closureVerified is manager-only)
        '404':
          description: Task not found for this client
  /scorecards:
    post:
      summary: Compute + persist the visit scorecard (S10) — field_agent only
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [visitId]
              properties:
                visitId: { type: string }
      responses:
        '201':
          description: >
            Scorecard upserted (idempotent). Dimension scores computed from the
            visit's S2-S8 rows, weighted by Client.scorecardWeights; ratingBand
            from Client.kpiThresholds (defaults green>=80, amber>=60).
        '400':
          description: Missing visitId
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Visit not found for this client
  /scorecards/{visitId}:
    get:
      summary: Fetch the scorecard for a visit
      security: [{ bearerAuth: [] }]
      parameters:
        - name: visitId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: The visit's scorecard
        '401':
          description: Missing or invalid token
        '404':
          description: Visit or scorecard not found for this client
  /dashboard:
    get:
      summary: Manager KPI rollup — manager/admin only
      security: [{ bearerAuth: [] }]
      parameters:
        - name: territoryId
          in: query
          schema: { type: string }
        - name: outletId
          in: query
          schema: { type: string }
        - name: from
          in: query
          schema: { type: string, format: date-time }
        - name: to
          in: query
          schema: { type: string, format: date-time }
      responses:
        '200':
          description: >
            { kpis: { numericDistribution, weightedDistribution, osaPct,
            executionScore, priceCompliancePct, visibilityCompliancePct,
            shareOfShelf, perfectStoreRate }, totals: { visits, outletsVisited,
            outletsTotal }, scoreBands: [{ label, minScore, outlets }] }.
            scoreBands is the perfect-store distribution — always five bands,
            highest first (90–100, 80–89, 70–79, 60–69, <60) — counting each
            outlet once, at its most recent scored visit in scope.
            weightedDistribution and shareOfShelf are Phase-1 proxies (see
            module comments).
        '400':
          description: Unparseable from/to date
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a manager/admin
  /photos:
    post:
      summary: Upload a photo for a visit (base64 data URL) — field_agent only
      description: >
        Phase-1 lean-infra storage (ADR 0007): the image is sent as a base64
        data URL and stored directly; real object storage is a later phase.
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [visitId, section, dataUrl, gpsTag, timestamp]
              properties:
                visitId: { type: string }
                section: { type: string, description: 'e.g. s3_visibility, s5_pricing, task_closure' }
                dataUrl: { type: string, description: 'base64 data URL, capped ~8MB' }
                gpsTag: { type: object }
                timestamp: { type: string, format: date-time }
      responses:
        '201':
          description: Photo stored
        '400':
          description: Missing/invalid fields or oversized dataUrl
        '401':
          description: Missing or invalid token
        '403':
          description: Caller is not a field_agent
        '404':
          description: Visit not found for this client
    get:
      summary: List a visit's photos
      security: [{ bearerAuth: [] }]
      parameters:
        - name: visitId
          in: query
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Photos for the visit (newest first)
        '400':
          description: Missing visitId
        '401':
          description: Missing or invalid token
        '404':
          description: Visit not found for this client
  # ── Phase 3 (Activation) ──────────────────────────────────────────────────
  # Full detail in docs/architecture/phase3-activation.md. All tenant-scoped
  # and role-guarded; responses summarised here.
  /territories:
    get:
      summary: List the client's territories
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Territories }, '401': { description: Unauthorized } }
    post:
      summary: Create a territory — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid }, '403': { description: Forbidden }, '409': { description: Duplicate code } }
  /territories/{id}/agents:
    post:
      summary: Assign an agent to a territory — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '201': { description: Assigned }, '404': { description: Territory or user not found }, '409': { description: Already assigned } }
  /territories/{id}/coverage:
    get:
      summary: Outlets + assigned agents for a territory
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Coverage }, '404': { description: Not found } }
  /campaigns:
    get:
      summary: List campaigns (with outlet counts)
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Campaigns } }
    post:
      summary: Create a campaign + assign outlets — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid }, '403': { description: Forbidden }, '404': { description: Outlet not found } }
  /campaigns/{id}:
    get:
      summary: Campaign + its outlets
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Campaign }, '404': { description: Not found } }
    patch:
      summary: Update campaign status/fields — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '403': { description: Forbidden }, '404': { description: Not found } }
  /campaigns/{id}/compliance:
    get:
      summary: Audit-derived compliance rollup — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Compliance rollup }, '404': { description: Not found } }
  /beatplans:
    get:
      summary: List beat plans (agent sees own; manager sees client's)
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: agentId, in: query, schema: { type: string } }
        - { name: status, in: query, schema: { type: string, enum: [planned, in_progress, completed] } }
      responses: { '200': { description: Beat plans }, '400': { description: Invalid status } }
    post:
      summary: Create a beat plan with sequenced stops — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid }, '403': { description: Forbidden }, '404': { description: Agent/outlet/territory not found } }
  /beatplans/{id}:
    get:
      summary: Beat plan + stops + adherence
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Beat plan }, '404': { description: Not found } }
    patch:
      summary: Update beat-plan status — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '404': { description: Not found } }
  /beatplans/{id}/stops/{stopId}:
    patch:
      summary: Mark a stop visited — field_agent/manager
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: stopId, in: path, required: true, schema: { type: string } }
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '404': { description: Not found } }
  /alerts/rules:
    get:
      summary: List alert rules
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Rules } }
    post:
      summary: Create an alert rule — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid metric }, '403': { description: Forbidden } }
  /alerts/rules/{id}:
    patch:
      summary: Update an alert rule — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '404': { description: Not found } }
  /alerts/evaluate:
    post:
      summary: Evaluate a visit against active rules → Alerts — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: 'Created alerts (may be empty)' }, '400': { description: Missing visitId }, '404': { description: Visit not found } }
  /alerts:
    get:
      summary: List alerts (filter acknowledged/severity)
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: acknowledged, in: query, schema: { type: string, enum: ['true', 'false'] } }
        - { name: severity, in: query, schema: { type: string } }
      responses: { '200': { description: Alerts } }
  /alerts/{id}/ack:
    patch:
      summary: Acknowledge an alert — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Acknowledged }, '404': { description: Not found } }
  /templates:
    get:
      summary: List audit templates (active; ?includeInactive=true for all)
      security: [{ bearerAuth: [] }]
      parameters: [{ name: includeInactive, in: query, schema: { type: boolean } }]
      responses: { '200': { description: Templates } }
    post:
      summary: Create an audit template — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid }, '403': { description: Forbidden } }
  /templates/{id}:
    get:
      summary: Fetch a template
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Template }, '404': { description: Not found } }
    patch:
      summary: Update a template (schema change bumps version) — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '404': { description: Not found } }
  /trends/scorecards:
    get:
      summary: Scorecard weighted-total trend (day/week) — manager/admin
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: interval, in: query, schema: { type: string, enum: [day, week] } }
      responses: { '200': { description: Time series }, '400': { description: Invalid params }, '403': { description: Forbidden } }
  /trends/availability:
    get:
      summary: On-shelf-availability % trend — manager/admin
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: interval, in: query, schema: { type: string, enum: [day, week] } }
      responses: { '200': { description: Time series }, '400': { description: Invalid params } }
  /trends/perfect-store:
    get:
      summary: Perfect-store (green scorecard) rate trend — manager/admin
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: interval, in: query, schema: { type: string, enum: [day, week] } }
      responses: { '200': { description: Time series }, '400': { description: Invalid params } }
  # ── Phase 2 (Intelligence) ────────────────────────────────────────────────
  /fraud/visits/{visitId}:
    get:
      summary: Fraud risk score + signals for a visit — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: visitId, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: 'Risk score + signals' }, '404': { description: Not found } }
  /fraud/attempts:
    get:
      summary: List check-in attempts (incl. rejected) — manager/admin
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: outletId, in: query, schema: { type: string } }
        - { name: agentId, in: query, schema: { type: string } }
        - { name: passed, in: query, schema: { type: string, enum: ['true', 'false'] } }
      responses: { '200': { description: Attempts } }
  /fraud/flagged:
    get:
      summary: Submitted visits above a fraud-risk threshold — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: minScore, in: query, schema: { type: number } }]
      responses: { '200': { description: Flagged visits } }
  /dispatch:
    post:
      summary: Rank agents for an outlet (nearest + in-territory) — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: 'Ranked candidates + recommended' }, '400': { description: Missing outletId }, '404': { description: Outlet not found } }
  /dispatch/agents:
    get:
      summary: Ranked agent candidates for an outlet — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: outletId, in: query, required: true, schema: { type: string } }]
      responses: { '200': { description: Candidates }, '400': { description: Missing outletId } }
  /forecast:
    get:
      summary: Exponential-smoothing demand forecast for a SKU — manager/admin
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: skuId, in: query, required: true, schema: { type: string } }
        - { name: outletId, in: query, schema: { type: string } }
      responses: { '200': { description: Forecast }, '400': { description: Missing skuId }, '404': { description: SKU not found } }
  # ── Phase 3 Tier-2 ────────────────────────────────────────────────────────
  /orders:
    get:
      summary: List orders (agent sees own; manager sees client's)
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: outletId, in: query, schema: { type: string } }
        - { name: status, in: query, schema: { type: string, enum: [submitted, confirmed, cancelled] } }
      responses: { '200': { description: Orders } }
    post:
      summary: Create an in-visit order / sell-in — field_agent/manager
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid }, '404': { description: Outlet/SKU/visit not found } }
  /orders/{id}:
    get:
      summary: Order + lines
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Order }, '404': { description: Not found } }
    patch:
      summary: Update order status — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '404': { description: Not found } }
  /messages:
    get:
      summary: List messages visible to the caller
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Messages } }
    post:
      summary: Send a direct or broadcast message
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Sent }, '400': { description: Empty body }, '404': { description: Recipient not found } }
  /messages/{id}/read:
    patch:
      summary: Mark a message read (recipient only)
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Marked read }, '404': { description: Not found } }
  /announcements:
    get:
      summary: List the client's announcements
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Announcements } }
    post:
      summary: Post an announcement — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid }, '403': { description: Forbidden } }
  /webhooks:
    get:
      summary: List webhook subscriptions — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Webhooks } }
    post:
      summary: Create a webhook subscription — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid url/event }, '403': { description: Forbidden } }
  /webhooks/{id}:
    patch:
      summary: Update a webhook — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '404': { description: Not found } }
    delete:
      summary: Delete a webhook — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '204': { description: Deleted }, '404': { description: Not found } }
  /gamification/leaderboard:
    get:
      summary: Field-agent leaderboard (points from scorecards/tasks/visits)
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
      responses: { '200': { description: Leaderboard }, '400': { description: Invalid dates } }
  /gamification/me:
    get:
      summary: The caller's own leaderboard entry + rank
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Entry } }
  /reports:
    get:
      summary: List saved report definitions — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Definitions } }
    post:
      summary: Create a report definition — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid type/filters }, '403': { description: Forbidden } }
  /reports/{id}:
    delete:
      summary: Delete a report definition — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '204': { description: Deleted }, '404': { description: Not found } }
  /reports/{id}/generate:
    get:
      summary: Run a report (JSON, or ?format=csv) — manager/admin
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: format, in: query, schema: { type: string, enum: [csv] } }
      responses: { '200': { description: 'Report rows (JSON or text/csv)' }, '404': { description: Not found } }
  # ── Follow-ups (Phase 1/3) ────────────────────────────────────────────────
  /clients/me:
    get:
      summary: The caller's client config (scorecard weights / KPI thresholds)
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Config } }
    patch:
      summary: Update scorecard weights / KPI thresholds — admin
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '403': { description: Forbidden } }
  /users:
    get:
      summary: List client users — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Users } }
    post:
      summary: Provision a user — admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid }, '403': { description: Forbidden }, '409': { description: Duplicate email } }
  /users/{id}:
    patch:
      summary: Update a user (active/role) — admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '404': { description: Not found } }
  /incentives:
    get:
      summary: List incentive schemes
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Schemes } }
    post:
      summary: Create an incentive scheme — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid metric }, '403': { description: Forbidden } }
  /incentives/earned:
    get:
      summary: Agents who met active scheme thresholds + reward points
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Earned incentives } }
  /incentives/{id}:
    patch:
      summary: Update an incentive scheme — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '404': { description: Not found } }
    delete:
      summary: Delete an incentive scheme — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '204': { description: Deleted }, '404': { description: Not found } }
  /report-schedules:
    get:
      summary: List report schedules — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '200': { description: Schedules } }
    post:
      summary: Schedule a saved report — manager/admin
      security: [{ bearerAuth: [] }]
      responses: { '201': { description: Created }, '400': { description: Invalid }, '404': { description: Report definition not found } }
  /report-schedules/{id}:
    patch:
      summary: Update a report schedule — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Updated }, '400': { description: Invalid }, '404': { description: Not found } }
    delete:
      summary: Delete a report schedule — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '204': { description: Deleted }, '404': { description: Not found } }
  /report-schedules/{id}/run:
    post:
      summary: Generate the scheduled report now + record the run — manager/admin
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: 'Generated (delivery is Phase 4)' }, '404': { description: Not found } }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
